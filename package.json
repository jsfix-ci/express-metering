{
  "name": "express-metering",
  "version": "0.2.0",
  "description": "simple middleware to meter and throttle API requests",
  "author": "Selvam Palanimalai <selvam.palanimalai@gmail.com>",
  "main": "index.js",
  "repository": "chartotu19/express-metering",
  "license": "MIT",
  "scripts": {
    "test": "mocha --recursive --bail --reported spec  test/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --recursive ",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --recursive test/"
  },
  "engines": {
    "node": ">= 4.0"
  },
  "dependencies": {
    "bluebird": "^3.4.7",
    "redis": "^2.6.5",
    "mongoose": "^4.8.1"
  },
  "devDependencies": {
    "assert": "^1.4.1",
    "coveralls": "^2.11.16",
    "express": "^4.14.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "sinon": "^1.17.7",
    "supertest": "^3.0.0"
  }
}
